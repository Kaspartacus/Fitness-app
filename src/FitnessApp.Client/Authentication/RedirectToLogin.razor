@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        if (!Navigation.Uri.EndsWith("/login", StringComparison.Ordinal))
        {
            Navigation.NavigateTo("/login", replace: true);
        }
    }
}
